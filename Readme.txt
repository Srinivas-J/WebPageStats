//Run instructions//

1. Update the require web pages URLs data at below Excel file under 'URL' column,
	"src/main/java/testdata/URLsTestData.xlsx"
	
2. Provide require configuration values like browser / column names at below,
	"src/main/java/config/config.json"
	
3. For run the page load timings test, navigate to below test class and run it.
	"src/test/java/testruns/WebpageLoadTimeTest.java'
	
4. After successful run, can check the application load times at below Excel under 'PageLoadTimeInSeconds' column,
    "src/main/java/testdata/URLsTestData.xlsx"